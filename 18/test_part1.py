

from tablet import Tablet

"""
def test_first_recovered_frequency():
    instructionStrings = ([
        'set a 1',
        'add a 2',
        'mul a a',
        'mod a 5',
        'snd a',
        'set a 0',
        'rcv a',
        'jgz a -1',
        'set a 1',
        'jgz a -2',
    ])

    t = Tablet(instructionStrings)

    t.run()

    assert t.getFirstRecoveredFrequency() == 4
"""
